{
  "name": "kaslev-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "KasLev protocol smart contracts — transparent, open-source high-leverage trading for the Kaspa EVM L2 ecosystem.",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy": "hardhat run scripts/deploy.js",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.17"
  }
}
